import {
  // cons, nil, showList
} from "./linkedlist"

describe('linkedlist', () => {
  // const myList = cons(1, cons(2, cons(3, nil)))

  it('should have JSON from linked list', () => {
    // expect(myList).toEqual({
    //   _tag: 'cons',
    //   head: 1,
    //   tail: {
    //     _tag: 'cons',
    //     head: 2,
    //     tail: {
    //       _tag: 'cons',
    //       head: 3,
    //       tail: { _tag: 'nil' }
    //     }
    //   }
    // })
  })

  it('should get list of number in string from linked list', () => {
    // expect(showList(myList)).toEqual('1, 2, 3')    
  })
})