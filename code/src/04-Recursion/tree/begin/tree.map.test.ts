// import { Tree } from "./tree"
// import { fmap } from "./tree.map"

// const double = (x: number) => x * 2

describe('Tree.map', () => {
  it('should map over a tree with an arbitrary function', () => {
    // const tree: Tree<number> = {
    //   _tag: 'branch', value: 1,
    //   left: { _tag: 'leaf', value: 2 },
    //   right: { _tag: 'leaf', value: 3 }
    // }
    // expect(fmap(double)(tree)).toEqual({
    //   _tag: 'branch', value: 2,
    //   left: { _tag: 'leaf', value: 4 },
    //   right: { _tag: 'leaf', value: 6 }
    // })
  })
})