describe('map.linkedlist', () => {
  it('should map over a linked list', () => {
    // const toLinkedList = cons(1, cons(2, cons(3, nil)))    
    // expect(
    //   fmap(double)(toLinkedList)
    // ).toEqual(
    //   cons(2, cons(4, cons(6, nil)))
    // )
  })
})